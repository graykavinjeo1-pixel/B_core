[package]
name = "synapse-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
name = "synapse_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true
