use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CoreEfficiencyBenchmark {
    pub efficiency_budget_targets_8gb_edge_device: bool,
    pub runtime_tiers_classify_always_on_and_on_demand_modules: bool,
    pub always_on_core_contains_only_minimal_kernel: bool,
    pub recursive_development_stack_is_on_demand_not_resident: bool,
    pub coding_training_arena_is_on_demand_not_resident: bool,
    pub patch_sandbox_is_on_demand_not_resident: bool,
    pub tool_layers_are_not_resident_by_default: bool,
    pub application_layers_are_not_resident_by_default: bool,
    pub active_field_budget_limits_active_field_size: bool,
    pub active_field_budget_limits_candidate_nodes: bool,
    pub memory_scale_audit_rejects_full_scan: bool,
    pub memory_scale_audit_keeps_latency_non_linear_to_total_nodes: bool,
    pub wake_controller_wakes_code_growth_only_on_development_goal: bool,
    pub sleep_controller_compresses_development_memory_after_task: bool,
    pub cold_storage_policy_drops_large_raw_logs_from_resident_memory: bool,
    pub device_profile_registers_raspberry_pi_8gb: bool,
    pub core_efficiency_benchmark_reduces_resident_memory_without_breaking_growth: bool,
    pub off_always_on_memory_mb: f32,
    pub on_always_on_memory_mb: f32,
    pub off_active_memory_mb: f32,
    pub on_active_memory_mb: f32,
    pub off_peak_memory_mb: f32,
    pub on_peak_memory_mb: f32,
    pub off_resident_module_count: usize,
    pub on_resident_module_count: usize,
    pub off_active_field_size: usize,
    pub on_active_field_size: usize,
    pub off_candidate_nodes: usize,
    pub on_candidate_nodes: usize,
    pub full_scan_used: bool,
    pub wake_cost_ms: f32,
    pub sleep_compression_score: f32,
    pub memory_scaling_score: f32,
    pub latency_growth_ratio: f32,
    pub core_efficiency_score: f32,
    pub recursive_stack_residency_reduction: f32,
}
