SEMANTIC REASONING PROJECT — SEM-32-R1
RELATIONAL TOPOLOGY GENERALIZATION REPAIR
ACCEPTANCE-HARNESS CORRECTION + FRESH BLIND CAUSAL WORLD REGATE

Continue ONLY the independent B_Core / Semantic Reasoning Project lineage.

Start from the corrected sealed SEM-32 post-acceptance-audit state:

CORRECTED_SEM32_COMMIT=
4a1040a3110d66ef5562c752afa84457c0ffd243

Historical canonical result is immutable:

SEM32_STATUS=FAIL
DOMINANT_BOUNDARY=RELATIONAL_DYNAMICS_LIMIT

NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=false

Do NOT rewrite historical SEM-32 as PASS.

Do NOT delete or overwrite the original canonical reports.

Do NOT start SEM-33.

Do NOT push unless explicitly authorized.

0. PURPOSE

SEM-32 established substantial temporal/causal world-model capability but failed the mandatory novel-relation-topology transfer condition.

SEM-32-R1 asks:

Can B_Core learn a semantic causal interaction mechanism from one relational organization and correctly reuse/compose that mechanism when the same semantic causal principles appear in a genuinely novel relation topology?

This is NOT:

memorize more graph shapes

It is:

learn relation-mediated causal semantics
+
bind them to fresh relational structures
+
compose them when topology changes
1. PRESERVE POSITIVE SEM-32 EVIDENCE AS BASELINE

Historical evidence includes:

ONE_STEP_PREDICTION=18/18
MULTISTEP_ERROR=0
COUNTERFACTUAL=3/3
AUTONOMOUS_INTERVENTIONS=5
100K_SPARSE_ROUTING_PASS=true
WORLD_MEMORY_FULL_SCANS=0

These are not sufficient for SEM-32 PASS because topology transfer failed.

Do NOT redesign mechanisms unrelated to the failed boundary unless autonomous diagnosis proves they are causally implicated.

2. PHASE P0 — ACCEPTANCE HARNESS REPAIR ONLY

Before any capability work:

repair the acceptance/evaluator infrastructure.

No reasoning-engine semantic changes are allowed in P0.

Correct the raw-field → level mapping.

Required invariant:

if NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=false
then SEM32_LEVEL_B_PASS=false
and SEM32_STATUS=FAIL

Create direct truth-table tests for every SEM-32 Level A–J dependency.

3. NEGATIVE ACCEPTANCE CANARY

Create a synthetic sealed report where every required field is PASS except:

NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=false

Required:

SEM32_LEVEL_B_PASS=false
SEM32_STATUS=FAIL

Also create at least one negative canary for each other mandatory Level dependency.

Required:

ACCEPTANCE_FALSE_PASS_EVENTS=0
4. BUDGET CONTRACT REPAIR

The SEM-32 specification required:

MAX_AUTONOMOUS_RESEARCH_EPOCHS=4096

but the canonical run used 512.

Repair orchestration so that:

CONFIGURED_HARD_CEILING=4096

exactly.

4096 is containment only.

It is NOT required consumption.

Early PASS / valid autonomous stop remains allowed.

Required:

BUDGET_IS_RESEARCH_SEMANTIC_INPUT=false

If campaign configuration does not contain the exact sealed hard ceiling:

STOP BEFORE CANONICAL EXPOSURE with:

CAMPAIGN_CONFIG_INVALID
5. SEAL P0 SEPARATELY

Record:

reasoning_engine_diff_in_P0=0
acceptance_harness_diff>0
orchestration_diff>0

Run infrastructure tests.

Only after P0 is sealed may capability diagnosis begin.

6. ACTUAL CAPABILITY QUESTION

The failed raw field indicates that B_Core's learned dynamics did not generalize to a novel relation topology.

Diagnose autonomously WHY.

Candidate hypotheses MAY include:

exact-topology binding

entity-position leakage

entity-ID leakage

fixed-cardinality assumptions

whole-graph-pattern memorization

relation-order dependence

insufficient role abstraction

insufficient local-mechanism composition

insufficient multi-hop relational propagation

context incorrectly bound to topology

routing failure on unseen adjacency structure

These are hypotheses only.

Do NOT assume any is the answer.

7. NO HUMAN-PRESCRIBED REPAIR

Required:

HUMAN_RELATIONAL_REPAIR_SELECTION_EVENTS=0
HUMAN_TOPOLOGY_TEMPLATE_SELECTION_EVENTS=0

Allow existing autonomous research machinery to:

observe failure
→ generate competing diagnoses
→ construct discriminating experiments
→ identify causal defect
→ synthesize bounded repair
→ verify

Do NOT manually select the repair architecture.

8. REQUIRED SEMANTIC CONTRACT

Whatever repair emerges, it must preserve this invariant:

Entity IDs and exact graph-instance identity are addresses/context, not the meaning of a causal mechanism.

Required:

ENTITY_ID_IS_CAUSAL_AUTHORITY=false
EXACT_GRAPH_INSTANCE_IS_CAUSAL_AUTHORITY=false

A relational mechanism's applicability must be determined by semantic roles/relations/context or equivalent general structure, not by memorized fixture identity.

9. LOCAL INTERACTION VS WHOLE-TOPOLOGY MEMORIZATION

Test whether an effect can be represented through reusable local relational mechanisms.

Conceptually:

entity-role A
   │ relation R1
entity-role B
   │ relation R2
entity-role C

may instantiate the same semantic mechanism under different concrete entity identities and larger surrounding graphs.

Do NOT require this exact representation.

This is an acceptance property, not an implementation prescription.

10. COMPOSITIONAL RELATIONAL DYNAMICS

A novel topology may require composition of previously verified interaction mechanisms.

Allow:

known local mechanism M1
+
known local mechanism M2
+
new relation topology
        ↓
new predicted global effect

without requiring an entirely new primitive law when existing mechanisms suffice.

Track:

RELATIONAL_MECHANISM_COMPOSITION_EVENTS
11. NOVEL TOPOLOGY IS NOT NOVEL RELATION TYPE

Keep separate:

KNOWN_RELATION_TYPES_IN_NEW_TOPOLOGY

from:

GENUINELY_NEW_RELATION_SEMANTICS

SEM-32-R1's primary gate concerns the former.

Do not mask topology-generalization failure by creating a new relation primitive for every unseen graph.

12. PERMUTATION / ID INVARIANCE CANARY

Before fresh canonical regate, validate that semantic-equivalent worlds remain equivalent under:

entity-ID permutation
storage-order permutation
unrelated entity insertion

where causal semantics are unchanged.

Required:

ENTITY_PERMUTATION_INVARIANCE_PASS=true
STORAGE_ORDER_INVARIANCE_PASS=true
13. CARDINALITY GENERALIZATION CANARY

Where the same local causal mechanism is semantically valid:

test it in worlds containing different numbers of unrelated or interacting entities.

Required:

ENTITY_CARDINALITY_GENERALIZATION_PASS=true

Do not require applicability where added entities genuinely alter the causal context.

14. CONTEXT-SENSITIVITY ANTI-OVERGENERALIZATION CANARY

The opposite test is mandatory.

Change a semantically relevant relation/context while keeping superficial topology similar.

The system must NOT transfer the law if applicability is broken.

Required:

RELATIONAL_OVERGENERALIZATION_EVENTS=0
15. TOPOLOGY FAMILY CANARIES

Use task-independent structural canaries before final freeze.

They should cover several qualitatively different relational structures, for example equivalents of:

chain
fork
converging paths
branching interaction
cycle
sparse disconnected context
additional distractor entities

These are evaluator structures only.

Do NOT hard-code topology names into B_Core reasoning.

The final fresh topology holdout must remain unseen.

16. STRUCTURAL DISTINCTNESS

Do not claim novel topology merely because IDs changed.

Measure topology distinctness using structural properties such as:

degree pattern
path structure
branching
relation arrangement
cycle structure
interaction locality
dependency structure

Exact metric is implementation-defined.

Required:

FRESH_TOPOLOGY_STRUCTURALLY_DISTINCT=true
17. SPARSE RELATIONAL ROUTING

Repair must preserve sparse behavior.

A fresh topology may change routing paths but must not trigger a full-world scan.

Required:

WORLD_MEMORY_FULL_SCANS=0
CAUSAL_MECHANISM_FULL_SCANS=0

Track:

ACTIVE_ENTITIES_P50/P95
ACTIVE_RELATIONS_P50/P95
ACTIVE_CAUSAL_MECHANISMS_P50/P95
18. NO WHOLE-GRAPH LOOKUP TABLE

Forbidden:

graph_hash → next_state
topology_id → causal_result
fixture_id → mechanism

Required:

TASK_INSTANCE_TRANSITION_CACHE_AUTHORITY=false
TOPOLOGY_HASH_LOOKUP_AUTHORITY=false
19. EXPOSED SEM-32 FAILURE MAY BE USED FOR DIAGNOSIS ONLY

The original failed topology is now exposed.

It may be used to determine:

why the old system failed

and to test whether a generic repair addresses that class of defect.

It must NOT be the sole evidence for successful generalization.

20. FREEZE REPAIR BEFORE FRESH HOLDOUT

After autonomous diagnosis and generic repair:

freeze:

engine hash
relational dynamics hash
routing hash
acceptance harness hash
campaign config hash
verifier hash
holdout-selection rule hash

Only then generate/select fresh relation-topology holdouts.

No capability changes after exposure.

21. FRESH BLIND REGATE

Use fresh structured worlds not used for repair.

Required characteristics:

known semantic relation vocabulary where appropriate

novel relational topology

fresh entity identities

fresh entity count where appropriate

fresh context arrangement

independently verified future transitions

Do NOT expose expected transitions or gold causal laws.

22. PRE-REPAIR BASELINE ARM

For strongest causality:

run the sealed pre-repair SEM-32 capability state on the same fresh holdout set.

Then run the frozen repaired descendant.

The repair must be frozen BEFORE either fresh-holdout result is revealed to the adaptive process.

Compare:

PRE_REPAIR_TOPOLOGY_TRANSFER
POST_REPAIR_TOPOLOGY_TRANSFER

This is evaluator-side comparison only.

23. PRIMARY R1 CAPABILITY GATE

Required:

NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=true

on fresh blind holdout.

This is mandatory.

No aggregate metric may override it.

24. RELATIONAL CAUSAL CORRECTNESS

For transferred mechanisms verify:

correct affected entities

correct unaffected entities

correct relation-mediated effects

correct context applicability

correct temporal delta

correct uncertainty where relevant

Do not pass based solely on partial overlap.

25. MULTI-HOP RELATIONAL EFFECT

Include at least one fresh case where the effect requires more than one relation-local reasoning step.

The system must compose mechanisms rather than rely on a direct memorized edge.

Track:

MULTIHOP_RELATIONAL_TRANSFER_EVENTS
26. NOVEL TOPOLOGY COUNTERFACTUAL

At least one counterfactual regate should alter:

action/intervention

inside a fresh relational topology.

Required:

NOVEL_TOPOLOGY_COUNTERFACTUAL_PASS=true

This ensures topology repair also survives causal imagination.

27. REACHABILITY REGRESSION

Re-run the unreachable-shortcut gate inside or adjacent to fresh relational structures.

Required:

UNREACHABLE_SHORTCUT_ACCEPTS=0

Relational generalization must not weaken reachability integrity.

28. UNCERTAINTY REGRESSION

If topology/context is ambiguous:

preserve epistemic uncertainty.

Required:

PREDICTIVE_UNCERTAINTY_COLLAPSE_EVENTS=0
29. INTERVENTIONAL CAUSALITY REGRESSION

Re-run at least one confounded observational case requiring intervention.

Required:

FALSE_CAUSAL_PROMOTIONS=0
INTERVENTIONAL_CAUSALITY_ABLATION_PASS=true
30. ALL SEM-32 LEVELS MUST REGATE

SEM-32-R1 is NOT a single-field patch acceptance.

After repair, re-evaluate all original Level A–J contracts on fresh/sealed data where required.

Required:

SEM32_R1_LEVEL_A_PASS=true
SEM32_R1_LEVEL_B_PASS=true
SEM32_R1_LEVEL_C_PASS=true
SEM32_R1_LEVEL_D_PASS=true
SEM32_R1_LEVEL_E_PASS=true
SEM32_R1_LEVEL_F_PASS=true
SEM32_R1_LEVEL_G_PASS=true
SEM32_R1_LEVEL_H_PASS=true
SEM32_R1_LEVEL_I_PASS=true
SEM32_R1_LEVEL_J_PASS=true
31. ACCEPTANCE HARNESS MUST READ RAW FIELDS

Final status must be derived mechanically from canonical raw fields.

No report-summary field may override raw evidence.

Required:

RAW_FIELD_ACCEPTANCE_AUTHORITY=true
32. ACCEPTANCE SELF-CHECK

At finalization, independently recompute:

raw fields
→ Level A–J
→ overall status

using a second simple acceptance implementation or equivalent mechanically independent check.

Required:

PRIMARY_SECONDARY_ACCEPTANCE_DIFF=0
33. CONFIGURATION SELF-CHECK

Before canonical run:

REQUESTED_MAX_AUTONOMOUS_RESEARCH_EPOCHS=4096
CONFIGURED_MAX_AUTONOMOUS_RESEARCH_EPOCHS=4096

Required:

CAMPAIGN_BUDGET_CONTRACT_PASS=true

Actual consumed epochs may be much lower.

34. AUTONOMOUS RESEARCH BUDGET

Use:

MAX_AUTONOMOUS_RESEARCH_EPOCHS=4096

with event-bounded early stop.

Track:

AUTONOMOUS_RESEARCH_EPOCHS_EXECUTED

Do not consume epochs after complete PASS evidence.

35. RESTART INVARIANCE

Preserve prior restart-safety requirement.

Required:

RESTART_CAUSALLY_AFFECTS_DIFFICULTY_DECISIONS=false
RESTART_CAUSALLY_AFFECTS_RELATIONAL_REASONING=false

where tested.

36. LITERATURE USE

Use the existing sealed SEM-32 literature audit only as mechanism-level research context.

Relevant families may include concepts from:

Interaction Networks
Graph relational dynamics
causal intervention literature
uncertainty-aware model-based prediction
reachability-aware world models

Do NOT transplant whole architectures.

Required:

WHOLE_ARCHITECTURE_TRANSPLANTS=0
37. NO NEW NEURAL DEPENDENCY

Do NOT add a large latent/neural simulator to solve topology transfer.

Required:

CORE_MANDATORY_VRAM=0
CORE_DEPENDS_ON_GPU_RUNTIME=false
38. NO LANGUAGE AUTHORITY

Required:

NATURAL_LANGUAGE_IS_CANONICAL_WORLD_MEMORY=false
NATURAL_LANGUAGE_IS_CAUSAL_REASONING_AUTHORITY=false
39. LEAKAGE

Required:

CAUSAL_GOLD_LAW_READS=0
EXPECTED_NEXT_STATE_LOOKUPS=0
FUTURE_WORLD_EVENT_LEAKAGE_EVENTS=0
COUNTERFACTUAL_GOLD_BRANCH_READS=0
FRESH_TOPOLOGY_GOLD_READS=0
40. REQUIRED CAUSAL ABLATION — RELATIONAL REPAIR

Create an equal-evidence comparison.

Arm A:

repaired relational-generalization mechanism enabled

Arm B:

same learned world knowledge
but claimed repair mechanism disabled

On fresh topology holdouts:

Arm B must materially lose the claimed topology transfer advantage.

Required:

RELATIONAL_TOPOLOGY_REPAIR_ABLATION_PASS=true
41. REQUIRED ANTI-MEMORIZATION ABLATION

Change:

entity IDs
storage order
irrelevant surroundings

while preserving semantic causal structure.

Predictions should remain equivalent.

Required:

ANTI_MEMORIZATION_ABLATION_PASS=true
42. REQUIRED ANTI-OVERGENERALIZATION ABLATION

Keep superficial graph shape similar but alter a causally relevant semantic relation/context.

Predictions must change appropriately.

Required:

ANTI_OVERGENERALIZATION_ABLATION_PASS=true
43. STRONG SUCCESS INTERPRETATION

The strongest R1 result is:

old system:
known topology → works
fresh topology → fails

autonomous diagnosis
        ↓
generic relational mechanism repair
        ↓
fresh unseen topology
        ↓
correct prediction
+
counterfactual
+
reachability
+
sparse routing
        ↓
targeted ablation removes advantage

This would establish actual relational-dynamics generalization rather than fixture memorization.

44. VALID FAILURE

If fresh topology still fails:

report the exact new boundary.

Possible dispositions:

ROLE_BINDING_LIMIT

RELATIONAL_COMPOSITION_LIMIT

MULTIHOP_DYNAMICS_LIMIT

TOPOLOGY_ROUTING_LIMIT

CARDINALITY_GENERALIZATION_LIMIT

CONTEXT_APPLICABILITY_LIMIT

RELATIONAL_CAUSAL_IDENTIFICATION_LIMIT

OTHER

Do NOT manually rescue after fresh exposure.

45. REQUIRED ARTIFACTS

Create at minimum:

reports/sem32_r1/

historical_sem32_fail_receipt.json

p0_acceptance_harness_repair.json
acceptance_truth_table_tests.json
budget_contract_audit.json

relational_failure_diagnosis.jsonl
diagnostic_experiments.jsonl
repair_hypotheses.jsonl
repair_lineage.jsonl

pre_freeze_relational_canaries.json
permutation_invariance.json
cardinality_generalization.json
anti_overgeneralization.json

repair_freeze.json

fresh_topology_manifest.json
fresh_topology_structural_distance.json

pre_repair_fresh_baseline.json
post_repair_fresh_results.json

novel_topology_counterfactual.json
multihop_relational_transfer.json

relational_repair_ablation.json
anti_memorization_ablation.json
anti_overgeneralization_ablation.json

all_levels_regate.json

primary_acceptance_result.json
secondary_acceptance_result.json

final_regression.json
clean_reconstruction.json

sem32_r1_final_report.json
SEM32_R1_REPORT.md
46. FINAL RESPONSE

Return:

SEM32_R1_STATUS=PASS|FAIL
DISPOSITION=

CAMPAIGN_ID=

BRANCH=
COMMIT=
WORKTREE_CLEAN=
PUSH_PERFORMED=

HISTORICAL_SEM32_STATUS=FAIL
HISTORICAL_SEM32_COMMIT=
CORRECTED_PREDECESSOR_COMMIT=

P0_ACCEPTANCE_HARNESS_REPAIRED=
P0_REASONING_ENGINE_DIFF=

ACCEPTANCE_FALSE_PASS_EVENTS=

REQUESTED_MAX_AUTONOMOUS_RESEARCH_EPOCHS=
CONFIGURED_MAX_AUTONOMOUS_RESEARCH_EPOCHS=
CAMPAIGN_BUDGET_CONTRACT_PASS=

AUTONOMOUS_RESEARCH_EPOCHS_EXECUTED=

RELATIONAL_FAILURE_DIAGNOSIS=

RELATIONAL_REPAIR_HYPOTHESES=
RELATIONAL_DIAGNOSTIC_EXPERIMENTS=
RELATIONAL_REPAIRS_IMPLEMENTED=
RELATIONAL_REPAIRS_ACCEPTED=

ENTITY_ID_IS_CAUSAL_AUTHORITY=
EXACT_GRAPH_INSTANCE_IS_CAUSAL_AUTHORITY=

ENTITY_PERMUTATION_INVARIANCE_PASS=
STORAGE_ORDER_INVARIANCE_PASS=
ENTITY_CARDINALITY_GENERALIZATION_PASS=

RELATIONAL_OVERGENERALIZATION_EVENTS=

FRESH_TOPOLOGY_STRUCTURALLY_DISTINCT=

PRE_REPAIR_NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=
POST_REPAIR_NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=

NOVEL_RELATION_TOPOLOGY_TRANSFER_PASS=

RELATIONAL_MECHANISM_COMPOSITION_EVENTS=
MULTIHOP_RELATIONAL_TRANSFER_EVENTS=

NOVEL_TOPOLOGY_COUNTERFACTUAL_PASS=

UNREACHABLE_SHORTCUT_ACCEPTS=
PREDICTIVE_UNCERTAINTY_COLLAPSE_EVENTS=
FALSE_CAUSAL_PROMOTIONS=

ACTIVE_ENTITIES_P50=
ACTIVE_ENTITIES_P95=
ACTIVE_RELATIONS_P50=
ACTIVE_RELATIONS_P95=
ACTIVE_CAUSAL_MECHANISMS_P50=
ACTIVE_CAUSAL_MECHANISMS_P95=

WORLD_MEMORY_FULL_SCANS=
CAUSAL_MECHANISM_FULL_SCANS=

TASK_INSTANCE_TRANSITION_CACHE_AUTHORITY=
TOPOLOGY_HASH_LOOKUP_AUTHORITY=

RELATIONAL_TOPOLOGY_REPAIR_ABLATION_PASS=
ANTI_MEMORIZATION_ABLATION_PASS=
ANTI_OVERGENERALIZATION_ABLATION_PASS=

RAW_FIELD_ACCEPTANCE_AUTHORITY=
PRIMARY_SECONDARY_ACCEPTANCE_DIFF=

SEM32_R1_LEVEL_A_PASS=
SEM32_R1_LEVEL_B_PASS=
SEM32_R1_LEVEL_C_PASS=
SEM32_R1_LEVEL_D_PASS=
SEM32_R1_LEVEL_E_PASS=
SEM32_R1_LEVEL_F_PASS=
SEM32_R1_LEVEL_G_PASS=
SEM32_R1_LEVEL_H_PASS=
SEM32_R1_LEVEL_I_PASS=
SEM32_R1_LEVEL_J_PASS=

CAUSAL_GOLD_LAW_READS=
EXPECTED_NEXT_STATE_LOOKUPS=
FUTURE_WORLD_EVENT_LEAKAGE_EVENTS=
COUNTERFACTUAL_GOLD_BRANCH_READS=
FRESH_TOPOLOGY_GOLD_READS=

WHOLE_ARCHITECTURE_TRANSPLANTS=

CORE_MANDATORY_VRAM=
CORE_DEPENDS_ON_GPU_RUNTIME=

GLOBAL_REASONING_REGRESSIONS=
META_QUALITY_REGRESSIONS=
GAIN_ERASURE_EVENTS=
CAPABILITY_NEGATIVE_TRANSFER_EVENTS=

NEW_CLIPPY_WARNING_SIGNATURES_TOTAL=
CORE_DOCKABILITY_PRESERVED=

NEXT_DOMINANT_GROWTH_LIMIT=

SEM33_STARTED=false
NEXT_ALLOWED_STAGE=OPERATOR_REVIEW_ONLY

Suggested commit:

Repair relational topology generalization and regate causal world model

Start SEM-32-R1 now.